import { StyleSheet } from "react-native";
import { W } from "../../../constants/window.constants";

export const styles = StyleSheet.create({
  container: { paddingRight: 40, paddingLeft: 40 },
  containerLogo: {
    paddingTop: 5,
    paddingBottom: 5,
  },
  imageLogo: {
    height: 55,
    aspectRatio: 1 / 1,
    resizeMode: "contain",
  },
  formContainer: {},
});
